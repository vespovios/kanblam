# App
NEXTAUTH_SECRET=replace-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000
APP_URL=http://localhost:3000

# Database
DATABASE_URL=postgresql://tasker:tasker@localhost:5432/tasker_test?schema=public

# Email (dev: MailHog on localhost)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Tasker <noreply@tasker.local>"

# Workspace seed (used only by prisma seed on first run)
WORKSPACE_NAME=Peter's Workspace
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me
